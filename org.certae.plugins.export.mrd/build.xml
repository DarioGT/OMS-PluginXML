<project default="makezip">
  <!-- set global properties for this build -->
  <property name="src" location="src"/>
  <property name="build" location="build"/>
  <property name="zipfile" value="org.certae.plugins.export.mrd.zip"/>
  <target name="init">
    <mkdir dir="${build}"/>
  </target>
    
  <target name="makezip" depends="init">
     <copy todir="build">
       <fileset dir="bin"/>
     </copy>
     
     <zip destfile="${zipfile}"
       basedir="build"
       update="true"/>
  
     <delete dir="${build}"/>
   </target>
</project>